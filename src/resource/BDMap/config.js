import { STYLE_RENDER_TYPE_INPUT } from '../../core/constants';

const config = [
  {
    label: '地址',
    key: 'address',
    initValue: '浙江省杭州市西湖区西湖',
    renderType: STYLE_RENDER_TYPE_INPUT,
  },
];

export default config;
